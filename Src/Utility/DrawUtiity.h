#pragma once
class DrawUtiity
{
public:
	static void DrawGraph(int x, int y, int tex, bool alpha, int secondTex = -1);
	static void DrawGraph(int x, int y, int psH, int tex, bool alpha, int secondTex = -1);
};

